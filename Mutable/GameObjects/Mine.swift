//
//  Mine.swift
//  Mutable
//
//  Created by Dylan Shackelford on 1/13/18.
//  Copyright © 2018 Dylan Shackelford. All rights reserved.
//

import Foundation
/*
class Mine: BasicObject
{
    init(positionInit : CGPoint, container : UIView, placeHolderButtonInit: UIButton)
    {
        super.init()
        self.setPosition(positionInit) //
        let size = CGSize(width:28,height:28)
        self.setSize(size)
        self.setImageFileName("mineImage2")
        self.setImage()
        container.insertSubview(self.getImage(), belowSubview: placeHolderButtonInit)
        objectArray.add(self)
    }
    
    override init!(restart positionInit: CGPoint, container: UIView!, placeholder placeHolderButtonInit: UIButton!, velocity velocityInit: CGVector) {
        
        super.init()
        self.setSize(CGSize(width: 28,height:28))
        self.setPosition(positionInit)
        self.setImageFileName("mineImage2")
        self.setImage()
        container.insertSubview(self.getImage(), belowSubview: placeHolderButtonInit)
        objectArray.add(self)
    }
    
    override func hide() {
        self.getImage().isHidden = true
    }
    
    override func show() {
        self.getImage().isHidden = false
    }
}
*/
